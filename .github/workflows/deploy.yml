name: Deploy to Hostinger

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies
        run: npm install

      - name: Clean previous build
        run: |
          rm -rf .next
          rm -rf out

      - name: Build Next.js
        run: npm run build

      - name: Check build output
        run: |
          echo "Total files in out:"
          find out -type f | wc -l
          echo "Root HTML files:"
          ls out/*.html

      - name: Upload build to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          source: "out"
          target: "/home/u568698356/deploy_temp"
          strip_components: 0
          rm: false

      - name: Move files to public_html via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          script: |
            echo "=== Clearing public_html ==="
            rm -rf /home/u568698356/public_html/*
            echo "=== Copying files ==="
            cp -r /home/u568698356/deploy_temp/out/. /home/u568698356/public_html/
            echo "=== Cleaning temp ==="
            rm -rf /home/u568698356/deploy_temp
            echo "=== Done! Files in public_html: ==="
            ls /home/u568698356/public_html/ | head -20
